                    val pkgSuffix = s.lastHookPkg?.let { pkg -> " · " + pkg } ?: ""
                    Text(
                        "Hook: $hs$pkgSuffix",
                        style = MaterialTheme.typography.bodySmall
                    )
                }
            }
        }
    }
}
